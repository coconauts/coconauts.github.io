version: '3.8'

services:
  jekyll:
    build:
      context: .
    ports:
      - "4000:4000"
    volumes:
      - .:/app
    environment:
      - JEKYLL_ENV=development
    stdin_open: true
    tty: true
